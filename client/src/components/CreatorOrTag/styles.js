import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  ul: {
    justifyContent: 'space-around',
  },
  
  title: {
    backgroundColor: '#fff',
    padding: '10px',
    borderRadius: '15px',
    width: 'auto',
    textAlign: 'center',
  },

}));
